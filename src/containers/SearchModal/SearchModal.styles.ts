import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
    panelStyles: {
        padding: "4px 0px",
        color: "#aaa",
    },
    input: {
        marginBottom: 15,
    },
    noResults: {
        padding: "10px 0",
    },
});
